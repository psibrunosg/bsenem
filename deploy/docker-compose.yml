version: '3.8'
services:
  app:
    image: php:8.2-cli
    volumes:
      - ../:/var/www/html
    working_dir: /var/www/html/backend/api
    command: php -S 0.0.0.0:8000 index.php
    ports:
      - "8000:8000"
    restart: unless-stopped
